Fix the Cart screen checkout UI overlap AND the Place Order functionality permanently.

There are currently TWO critical issues on the Cart / Checkout flow.

==================================================
ISSUE 1: CHECKOUT / PLACE ORDER BUTTON IS HIDDEN
==================================================

Current bug:
On the Cart screen, when products are added to the cart, the green Checkout / Place Order button at the bottom is almost completely hidden behind the fixed bottom tab navigation bar.

Only the top edge of the green button is visible.

Expected behavior:
- The full Checkout / Place Order button must always be visible.
- The button must appear ABOVE the app bottom tab navigation bar.
- It must never overlap with Home, Categories, Cart, or Account navigation.
- Respect Android system navigation bar and safe area.
- The full button must be clearly visible and clickable.
- Keep the current Cart UI design unchanged.

Investigate:
- absolute/fixed positioning on checkout footer
- bottom: 0 ignoring tab bar height
- missing SafeAreaView or safe-area inset
- missing ScrollView bottom padding
- checkout footer rendered behind custom bottom navigation
- incorrect zIndex/elevation
- parent overflow: 'hidden'
- hardcoded bottom spacing
- Android navigation inset handling

Do NOT use random marginBottom or device-specific hardcoded values as a temporary workaround.

Use proper safe-area and bottom-tab positioning.

If React Navigation bottom tabs are used:

import { useBottomTabBarHeight } from '@react-navigation/bottom-tabs';
import { useSafeAreaInsets } from 'react-native-safe-area-context';

const tabBarHeight = useBottomTabBarHeight();
const insets = useSafeAreaInsets();

Position the sticky checkout footer ABOVE the actual tab bar.

Also add sufficient bottom padding to the Cart ScrollView so the Order Summary and checkout content are never covered.

Use the Home screen floating "4 items in cart" bar as a positioning reference because it is currently displayed correctly above the bottom navigation.

Expected visual structure:

[ Cart Items ]

[ Apply Coupon ]

[ Order Summary ]

[ FULL GREEN PLACE ORDER / PROCEED TO CHECKOUT BUTTON ]

--------------------------------
[ Home | Categories | Cart | Account ]
--------------------------------
[ Android System Navigation Bar ]


==================================================
ISSUE 2: PLACE ORDER BUTTON DOES NOT PLACE THE ORDER
==================================================

Current critical bug:
When the user clicks the "Place Order" button, the order is NOT being placed.

The button may respond visually, but no actual order is created and the checkout flow does not complete.

Diagnose the complete order placement flow from frontend to Supabase/database.

Do NOT create a fake success screen or only navigate to an Order Success page.

An order must be successfully created in the database before showing success.

Investigate specifically:

1. PLACE ORDER BUTTON EVENT

Verify the Place Order button has the correct onPress handler.

Example:

<TouchableOpacity
  onPress={handlePlaceOrder}
  disabled={isPlacingOrder}
>
  <Text>
    {isPlacingOrder ? 'Placing Order...' : 'Place Order'}
  </Text>
</TouchableOpacity>

Make sure:
- handlePlaceOrder is actually called
- no invisible View is blocking the button
- pointerEvents is not blocking interaction
- bottom navigation is not intercepting the tap
- the button is not permanently disabled
- validation is not silently returning
- errors are not swallowed


2. USER AUTHENTICATION

Before placing an order, get the current authenticated Supabase user.

Example:

const {
  data: { user },
  error: userError,
} = await supabase.auth.getUser();

If no authenticated user exists:
- do not silently fail
- show a clear error message
- redirect to Login only if required by the existing app flow

Use the real authenticated user.id.

Never use:
- hardcoded user IDs
- demo user IDs
- fake UUIDs
- mock users


3. VALIDATE CART

Before creating the order:

- Verify cart is not empty.
- Verify every cart item has a valid product ID.
- Verify quantity is greater than 0.
- Verify product still exists.
- Verify required pricing information exists.

If validation fails, show a meaningful error.

Do not silently return from handlePlaceOrder.


4. CREATE ORDER IN SUPABASE

Create a real order record in the orders table.

Use the existing database schema if it is correct.

Do not create duplicate tables without first inspecting the current Supabase schema.

The order should contain appropriate fields based on the existing schema, such as:

- id
- user_id
- order_number
- subtotal
- gst_amount
- delivery_charge
- discount_amount
- total_amount
- order_status
- payment_status
- payment_method
- delivery_address
- created_at
- updated_at

Use the actual authenticated user ID.

Generate a unique order number if required.

Example format:

ORD-YYYYMMDD-RANDOM

Example:
ORD-20260704-A8F2


5. CREATE ORDER ITEMS

After the order is successfully created, insert every cart product into the order_items table.

Each order item should reference the newly created order ID.

Store fields according to the existing schema, such as:

- order_id
- product_id
- product_name
- brand_name
- quantity
- unit_price
- gst_rate
- item_total
- created_at

IMPORTANT:
Use the price stored/validated for the order flow.

Do not accidentally use the original MRP when the cart is showing the selling price.

Example:

Metformin 500mg
Selling price: ₹32
Quantity: 2

Order item total must be:
₹32 × 2 = ₹64


6. USE ATOMIC ORDER CREATION IF POSSIBLE

Order creation and order_items creation must not leave partial or broken orders.

Preferred approach:
Create a Supabase PostgreSQL RPC/database function to place the order transactionally.

The transaction should:

BEGIN

1. Validate the authenticated user.
2. Validate cart items/product IDs.
3. Calculate or verify totals.
4. Insert the order.
5. Insert all order_items.
6. Return the created order ID and order number.

COMMIT

If any step fails:

ROLLBACK

Do not leave an empty order record if order_items insertion fails.

If an RPC is not appropriate for the existing architecture, implement robust cleanup/error handling while preserving the current database design.


7. CHECK SUPABASE RLS POLICIES

Inspect Row Level Security policies on:

- orders
- order_items
- cart
- cart_items
- profiles

A common reason for the Place Order button silently failing is Supabase RLS blocking INSERT operations.

Verify authenticated users can:

- INSERT their own order
- SELECT their own orders
- INSERT order_items for their own orders
- SELECT items belonging to their own orders

Example policy logic should correctly use:

auth.uid()

and compare it with the order's user_id.

Do NOT disable RLS globally as a shortcut.

Do NOT use the Supabase service role key in the React Native mobile app.

Never expose the service_role key or other server secrets in client-side code.


8. ERROR HANDLING AND DEBUGGING

Wrap handlePlaceOrder in try/catch.

Log the actual Supabase error during development.

Example:

try {
  setIsPlacingOrder(true);

  // Place real order

} catch (error) {
  console.error('PLACE ORDER ERROR:', error);

  Alert.alert(
    'Order Failed',
    error?.message || 'Unable to place your order. Please try again.'
  );

} finally {
  setIsPlacingOrder(false);
}

Do not use empty catch blocks.

Do not silently ignore:
- Supabase errors
- database insert errors
- RLS errors
- invalid UUID errors
- foreign key errors
- null constraint errors

Check the Expo/Replit console and identify the exact root error.


9. PREVENT DUPLICATE ORDERS

While the order is being created:

- disable the Place Order button
- show "Placing Order..."
- prevent multiple taps
- use an isPlacingOrder state

The user must not accidentally create multiple orders by rapidly tapping the button.


10. SUCCESS FLOW

ONLY after:
- the order is successfully inserted
- all order_items are successfully inserted

Then:

1. Clear the user's cart.
2. Reset local cart state.
3. Update the cart badge count to 0.
4. Show Order Placed Successfully.
5. Navigate to Order Success or Order Details screen.
6. Pass the REAL created order ID/order number.
7. The new order must appear in My Orders / Order History.

Do NOT clear the cart before successful database order creation.

Do NOT show success if Supabase insertion fails.


11. MY ORDERS INTEGRATION

After placing the order, verify the newly created order appears in the user's My Orders screen.

My Orders must fetch real orders from Supabase using the authenticated user's ID.

Remove any remaining:
- demo orders
- mock orders
- hardcoded order history
- local fake order data

Order history must be database-driven.


==================================================
IMPORTANT DATABASE RULES
==================================================

The app was previously connected to demo/mock data.

Inspect the entire Cart -> Checkout -> Place Order -> My Orders flow.

Remove any leftover demo/mock order placement logic.

Use the existing Supabase project as the single source of truth.

Do not store new orders only in:
- local React state
- AsyncStorage
- hardcoded arrays
- mock JSON files

The real order must persist in Supabase.

Do not delete existing real user data.

Do not redesign the UI.

Do not change product cards, colors, typography, bottom navigation, or Cart screen styling except the required checkout button positioning fix.


==================================================
FINAL END-TO-END TEST
==================================================

Perform this exact test:

1. Login with a real authenticated user.
2. Add Amoxicillin 500mg to cart.
3. Add Metformin 500mg quantity 2.
4. Open Cart.
5. Verify the cart badge shows the correct quantity/count according to the existing app logic.
6. Scroll to the bottom.
7. Verify the FULL Place Order button is visible above bottom navigation.
8. Tap Place Order.
9. Verify the button changes to "Placing Order...".
10. Verify exactly ONE order is created in Supabase.
11. Verify order_items contains all cart products.
12. Verify Metformin quantity is 2.
13. Verify Metformin item total is ₹64 when unit price is ₹32.
14. Verify subtotal, GST, delivery, discount, and final total are correct.
15. Verify the cart is cleared only after successful order creation.
16. Verify cart badge updates.
17. Verify Order Success screen receives the real order ID.
18. Open My Orders.
19. Verify the newly placed order appears.
20. Restart the Expo app.
21. Verify the order still exists in My Orders.

Fix both issues completely and verify the entire real Supabase order flow.

Do not stop after making code changes. Inspect errors, test the flow, and fix all related issues until Place Order successfully creates and persists a real order.